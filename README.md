# Daliary (달이어리) 🌙

커플을 위한 감성 다이어리 및 여행 기록 플랫폼입니다.

## ✨ 주요 기능

- **오늘의 질문**: 매일 새로운 질문에 답변하며 서로를 알아가는 공간
- **타임라인**: 소중한 일상을 기록하고 공유하는 커플 다이어리
- **여행 지도**: 함께 방문한 지역을 인증하고 지도를 채워가는 즐거움
- **가고 싶은 곳**: 함께 가고 싶은 장소들을 시/도 단위로 관리하는 위시리스트
- **하트 게이지**: 활동에 따라 오르는 레벨 시스템과 포인트 기록
- **출석체크**: 매일 접속하여 포인트를 쌓는 출석 시스템

## 🛠 기술 스택

- **Frontend**: React, TypeScript, Vite, Tailwind CSS, Framer Motion
- **Backend/Database**: Supabase (PostgreSQL, Auth, Storage)
- **Icons**: Lucide React

## 🚀 시작하기

1. **의존성 설치**

   ```bash
   npm install
   ```

2. **환경 변수 설정**
   `.env` 파일을 생성하고 Supabase 정보를 입력합니다.

   ```env
   VITE_SUPABASE_URL=your_supabase_url
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

3. **데이터베이스 설정**
   `supabase/migrations/` 폴더 내의 SQL 파일들을 Supabase SQL Editor에서 실행합니다.

4. **개발 서버 실행**
   ```bash
   npm run dev
   ```

## 📂 프로젝트 구조

- `src/components`: 재사용 가능한 UI 컴포넌트
- `src/context`: 전역 상태 관리 (Couple, Places)
- `src/hooks`: 커스텀 훅 및 API 로직
- `src/pages`: 각 서비스 페이지
- `supabase/`: 데이터베이스 마이그레이션 및 시드 데이터
